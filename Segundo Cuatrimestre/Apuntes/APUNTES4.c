#include <stdio.h>
#define CANT_MAX 5

int main (void){
    char txt[]=( 'h' , 'o' , 'l' , 'a');
    int=0
    printf("%s", txt); // el string se escribe directamente es una forma mas corta
   // while(txt{i}!=0)printf("%c")  (vector de tipo char que acepta valores ascci, excepto cero porque seria un null)
   // for(int i=0; i<4;i++){
     //   printf("%c",txt[i]);
    }
    return 0;

}